"""Test 3 - known functional vs constitutive mutants must be separated by the hard gates."""
import pytest


@pytest.mark.skip(reason="TODO(E): needs the full pipeline")
def test_known_dead_mutants_are_gated_out():
    ...
